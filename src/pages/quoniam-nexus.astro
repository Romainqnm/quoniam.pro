---
import { ViewTransitions } from 'astro:transitions';
---

<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Quoniam Nexus</title>
    <ViewTransitions />
    <style>
      :root { view-transition-name: root; }
      ::view-transition-old(root),
      ::view-transition-new(root) {
        animation-duration: 560ms;
        animation-timing-function: cubic-bezier(.22,.88,.22,1);
      }
      ::view-transition-new(root) {
        animation-name: nexusReveal;
      }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        min-height: 100vh;
        color: #e8f8ff;
        font-family: 'Segoe UI', system-ui, sans-serif;
        background: radial-gradient(circle at 50% 30%, #083067 0%, #06193a 50%, #040d20 100%);
        overflow: hidden;
      }
      .wrap { min-height: 100vh; display: grid; place-items: center; padding: 28px; position: relative; }
      .matrix {
        position: absolute; inset: 42% 6% -4% 6%;
        background:
          linear-gradient(rgba(122,221,255,.36) 1px, transparent 1px),
          linear-gradient(90deg, rgba(122,221,255,.36) 1px, transparent 1px);
        background-size: 32px 32px;
        transform: perspective(700px) rotateX(68deg);
        animation: rush .7s linear infinite;
      }
      .nodes { position: absolute; inset: 54% 8% 10% 8%; }
      .nodes i {
        position: absolute; width: 12px; height: 12px; border-radius: 50%;
        background: #f4fdff; box-shadow: 0 0 16px rgba(122,221,255,.9);
        animation: pulse 1.1s ease-in-out infinite;
      }
      .nodes i::after {
        content: ""; position: absolute; top: 50%; left: 50%; width: 110px; height: 2px;
        transform-origin: left center; transform: rotate(var(--a)) scaleX(0);
        background: linear-gradient(90deg, rgba(228,251,255,.95), rgba(122,221,255,0));
        animation: link 1.1s ease-in-out infinite;
      }
      .nodes i:nth-child(1){top:8%;left:12%;--a:16deg}
      .nodes i:nth-child(2){top:34%;left:44%;--a:-18deg;animation-delay:-.25s}
      .nodes i:nth-child(3){top:58%;left:74%;--a:22deg;animation-delay:-.5s}
      .nodes i:nth-child(4){top:74%;left:26%;--a:-24deg;animation-delay:-.75s}
      .card {
        view-transition-name: nexus-card;
        position: relative; z-index: 1; max-width: 760px; border-radius: 24px; padding: 30px;
        background: linear-gradient(140deg, rgba(255,255,255,.16), rgba(255,255,255,.03));
        border: 1px solid rgba(170,225,255,.34); backdrop-filter: blur(14px);
        box-shadow: 0 26px 60px rgba(0,0,0,.58), 0 0 72px rgba(74,196,255,.36);
      }
      h1 { margin: 0; font-size: clamp(2rem, 5vw, 3.2rem); background: linear-gradient(90deg, #fff, #8cdfff, #fff); -webkit-background-clip:text; -webkit-text-fill-color:transparent; }
      p { color: #dff3ff; line-height: 1.62; }
      a { color: #d6f2ff; text-decoration: none; font-weight: 600; }
      @keyframes rush { from{background-position:0 0,0 0} to{background-position:0 34px,34px 0} }
      @keyframes pulse { 0%,100%{transform:scale(.84);opacity:.66} 50%{transform:scale(1.22);opacity:1} }
      @keyframes link { 0%,100%{transform:rotate(var(--a)) scaleX(.06);opacity:.3} 45%{transform:rotate(var(--a)) scaleX(1);opacity:1} }
      @keyframes nexusReveal {
        0% { filter: saturate(1.35) contrast(1.12); box-shadow: inset 0 0 190px rgba(102,220,255,.24); }
        100% { filter: none; box-shadow: inset 0 0 0 rgba(102,220,255,0); }
      }
    </style>
  </head>
  <body>
    <main class="wrap">
      <div class="matrix" aria-hidden="true"></div>
      <div class="nodes" aria-hidden="true"><i></i><i></i><i></i><i></i></div>
      <section class="card">
        <h1>Quoniam Nexus</h1>
        <p>Module d’optimisation et d’automatisation orienté performance : flux data ultra-rapides, nœuds connectés et sensation d’hyper-vitesse système.</p>
        <a href="/">← Retour au hub</a>
      </section>
    </main>
  </body>
</html>
