---
import { ViewTransitions } from 'astro:transitions';
---

<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Quoniam Prisma</title>
    <ViewTransitions />
    <style>
      :root { view-transition-name: root; }
      ::view-transition-old(root),
      ::view-transition-new(root) {
        animation-duration: 560ms;
        animation-timing-function: cubic-bezier(.22,.88,.22,1);
      }
      ::view-transition-new(root) {
        animation-name: prismaReveal;
      }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        min-height: 100vh;
        font-family: 'Segoe UI', system-ui, sans-serif;
        color: #fff;
        background: radial-gradient(circle at 50% 30%, #2d355e 0%, #171c38 45%, #0d1022 100%);
        overflow: hidden;
      }
      .wrap { min-height: 100vh; display: grid; place-items: center; padding: 28px; position: relative; }
      .beams {
        position: absolute; inset: -15% -35%;
        background:
          linear-gradient(108deg, rgba(255,255,255,.52), rgba(255,255,255,0) 42%),
          linear-gradient(118deg, rgba(132,241,255,.30), rgba(255,255,255,0) 48%),
          linear-gradient(128deg, rgba(255,132,228,.28), rgba(255,255,255,0) 54%),
          linear-gradient(138deg, rgba(255,245,171,.24), rgba(255,255,255,0) 58%);
        animation: split 2.4s linear infinite;
        mix-blend-mode: screen;
      }
      .shards { position: absolute; inset: 10% 10%; }
      .shards i {
        position: absolute; width: 56px; height: 56px;
        clip-path: polygon(50% 0, 100% 38%, 74% 100%, 24% 100%, 0 38%);
        background: linear-gradient(150deg, rgba(255,255,255,.66), rgba(132,241,255,.18));
        opacity: .24; animation: float 5.6s ease-in-out infinite;
      }
      .shards i:nth-child(1){top:18%;left:14%}.shards i:nth-child(2){top:62%;left:74%;animation-delay:-2s}.shards i:nth-child(3){top:24%;left:78%;animation-delay:-3.1s}
      .card {
        view-transition-name: prisma-card;
        position: relative; z-index: 1; max-width: 760px; border-radius: 24px; padding: 30px;
        background: linear-gradient(145deg, rgba(255,255,255,.25), rgba(255,255,255,.04));
        border: 1px solid rgba(255,255,255,.4); backdrop-filter: blur(18px) saturate(130%);
        box-shadow: 0 26px 60px rgba(0,0,0,.54), 0 0 64px rgba(220,245,255,.34);
      }
      h1 { margin: 0; font-size: clamp(2rem, 5vw, 3.2rem); color: #fff; text-shadow: 0 0 20px rgba(255,255,255,.45); }
      p { color: #f4f8ff; line-height: 1.65; }
      a { color: #dff6ff; text-decoration: none; font-weight: 600; }
      @keyframes split { 0%,100%{transform:translateX(-6%) rotate(-2deg)} 50%{transform:translateX(3%) rotate(0deg)} }
      @keyframes float { 0%,100%{transform:translateY(0) rotate(0deg) scale(.85)} 50%{transform:translateY(-14px) rotate(24deg) scale(1.08)} }
      @keyframes prismaReveal {
        0% { filter: brightness(1.1) saturate(1.2); box-shadow: inset 0 0 200px rgba(255,255,255,.24); }
        100% { filter: none; box-shadow: inset 0 0 0 rgba(255,255,255,0); }
      }
    </style>
  </head>
  <body>
    <main class="wrap">
      <div class="beams" aria-hidden="true"></div>
      <div class="shards" aria-hidden="true"><i></i><i></i><i></i></div>
      <section class="card">
        <h1>Quoniam Prisma</h1>
        <p>Dimension cristal dédiée au traitement d’image : lumière blanche réfractée, aberration chromatique maîtrisée et éclats en mouvement.</p>
        <a href="/">← Retour au hub</a>
      </section>
    </main>
  </body>
</html>
