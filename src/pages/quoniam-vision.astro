---
import { ViewTransitions } from 'astro:transitions';
---

<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Quoniam Vision</title>
    <ViewTransitions />
    <style>
      :root { --f:#ff4fd8; --v:#8f3fff; --c:#55ecff; }
      :root { view-transition-name: root; }
      ::view-transition-old(root),
      ::view-transition-new(root) {
        animation-duration: 560ms;
        animation-timing-function: cubic-bezier(.22,.88,.22,1);
      }
      ::view-transition-new(root) {
        animation-name: visionReveal;
      }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        min-height: 100vh;
        color: #f7f0ff;
        font-family: 'Segoe UI', system-ui, sans-serif;
        background: radial-gradient(circle at 50% 40%, #25103f 0%, #0d0a22 55%, #070512 100%);
        overflow: hidden;
      }
      .wrap { position: relative; min-height: 100vh; display: grid; place-items: center; padding: 28px; }
      .tunnel { position: absolute; width: min(85vmin, 760px); aspect-ratio: 1; }
      .tunnel span {
        position: absolute; inset: 0; border-radius: 50%; border: 2px solid rgba(255,79,216,.55);
        box-shadow: 0 0 18px rgba(85,236,255,.34), inset 0 0 16px rgba(143,63,255,.18);
        transform: scale(.12); opacity: 0; animation: rush 3.1s linear infinite;
      }
      .tunnel span:nth-child(2){animation-delay:.4s}.tunnel span:nth-child(3){animation-delay:.8s}.tunnel span:nth-child(4){animation-delay:1.2s}.tunnel span:nth-child(5){animation-delay:1.6s}
      .freq {
        position: absolute; inset: auto 10% 12% 10%; height: 90px;
        background: repeating-linear-gradient(90deg, rgba(85,236,255,.85) 0 2px, transparent 2px 14px);
        mask-image: linear-gradient(90deg, transparent, #000 22%, #000 78%, transparent);
        animation: drift 1.8s linear infinite;
      }
      .card {
        view-transition-name: vision-card;
        position: relative; z-index: 1; max-width: 760px; border-radius: 24px; padding: 28px;
        background: linear-gradient(140deg, rgba(255,255,255,.14), rgba(255,255,255,.03));
        border: 1px solid rgba(181,204,255,.28); backdrop-filter: blur(14px);
        box-shadow: 0 24px 58px rgba(0,0,0,.55), 0 0 74px rgba(143,63,255,.28);
      }
      h1 { margin: 0; font-size: clamp(2rem, 5vw, 3.2rem); background: linear-gradient(90deg, var(--f), var(--v), var(--c)); -webkit-background-clip:text; -webkit-text-fill-color:transparent; }
      p { color: #e6dcff; line-height: 1.6; }
      a { color: #ffd3ff; text-decoration: none; font-weight: 600; }
      @keyframes rush { 0%{transform:scale(.12);opacity:0} 18%{opacity:.95} 100%{transform:scale(1.28);opacity:0} }
      @keyframes drift { from{background-position:0 0} to{background-position:64px 0} }
      @keyframes visionReveal {
        0% { filter: saturate(1.5) contrast(1.15); box-shadow: inset 0 0 180px rgba(255,79,216,.28); }
        100% { filter: none; box-shadow: inset 0 0 0 rgba(255,79,216,0); }
      }
    </style>
  </head>
  <body>
    <main class="wrap">
      <div class="tunnel" aria-hidden="true"><span></span><span></span><span></span><span></span><span></span></div>
      <div class="freq" aria-hidden="true"></div>
      <section class="card">
        <h1>Quoniam Vision</h1>
        <p>Portail synthwave de visualisation audio : fréquences dynamiques, perspective tunnel et impulsions néon pour observer le son en profondeur.</p>
        <a href="/">← Retour au hub</a>
      </section>
    </main>
  </body>
</html>
